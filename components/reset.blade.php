<div class="form-group">
    <div class="col-md-6 col-md-offset-4">
        <button type="reset" class="btn btn-{{ isset($buttonClass) ? $buttonClass : 'primary' }}">
            {{ $name }}
        </button>
    </div>
</div>
